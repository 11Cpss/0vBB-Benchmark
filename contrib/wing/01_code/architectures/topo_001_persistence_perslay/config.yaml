architecture_id: topo_001_persistence_perslay

data:
  root: /home/klz/Data/zeronu_benchmark/NEXT
  max_files_per_class: 100
  split_seed: 42
  split_fractions: [0.8, 0.1, 0.1]
  num_workers: 0
  balance_training_classes: true
  event_shuffle_buffer_size: 512

representation:
  point_bin_size: 15.0
  coordinate_scale: 1000.0
  max_points: 512

model:
  feature_dim: 2
  max_topology_points: 96
  diagram_hidden_dim: 96
  embedding_dim: 128
  classifier_dim: 128
  dropout: 0.10

training:
  batch_size: 16
  epochs: 50
  learning_rate: 0.0007
  weight_decay: 0.0001
  gradient_clip_norm: 1.0
  early_stopping_patience: 12
  early_stopping_min_delta: 0.0
  seed: 42
  deterministic: false
  use_amp: true
  amp_precision: auto
